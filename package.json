{
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "cd server && npm start"
  },
   "dependencies": {
    "npm": "^11.4.1",
    "start": "^5.1.0"
  }
}
